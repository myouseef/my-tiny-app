# اختبار Supabase Auth 🧪

## الحالة الحالية

✅ Frontend يعمل على: http://localhost:5173  
✅ Supabase Client مُعد  
✅ المستخدم التجريبي موجود  

---

## خطوات الاختبار

### 1. افتح التطبيق
```
http://localhost:5173
```

### 2. اذهب إلى صفحة Login
اضغط على "تسجيل الدخول" أو اذهب مباشرة إلى:
```
http://localhost:5173/login
```

### 3. أدخل بيانات المستخدم التجريبي
```
Email: demo@example.com
Password: demo123
```

أو اضغط على "استخدام المستخدم التجريبي"

### 4. اضغط "تسجيل الدخول"

---

## النتائج المتوقعة

### ✅ إذا نجح:
1. رسالة "تم تسجيل الدخول بنجاح!"
2. إعادة توجيه إلى Dashboard
3. عرض معلومات المستخدم:
   - البريد الإلكتروني: demo@example.com
   - معرف المستخدم
   - تاريخ الإنشاء
   - آخر تسجيل دخول

### ❌ إذا فشل:
- رسالة خطأ: "البريد الإلكتروني أو كلمة المرور غير صحيحة"
- **الحل:** تأكد من إنشاء المستخدم في Supabase مع "Auto Confirm User" مفعّل

---

## اختبارات إضافية

### اختبار 1: تسجيل الخروج
1. في Dashboard، اضغط "تسجيل الخروج"
2. يجب أن تعود للصفحة الرئيسية

### اختبار 2: حماية المسارات
1. بعد تسجيل الخروج، حاول الوصول مباشرة إلى:
   ```
   http://localhost:5173/dashboard
   ```
2. يجب أن يتم توجيهك تلقائياً إلى صفحة Login

### اختبار 3: Developer Console
1. افتح Developer Console (F12)
2. تحقق من عدم وجود أخطاء
3. يجب أن ترى رسائل مثل:
   ```
   ✅ تسجيل دخول ناجح: demo@example.com
   ✅ تم جلب بيانات المستخدم: demo@example.com
   ```

---

## استكشاف الأخطاء

### ❌ خطأ: "Invalid login credentials"
**السبب:** المستخدم غير موجود أو كلمة المرور خاطئة  
**الحل:**
1. تحقق من Supabase Dashboard → Authentication → Users
2. تأكد من وجود demo@example.com
3. تأكد من أن Status = "Confirmed"

### ❌ خطأ: "supabaseUrl is required"
**السبب:** Environment variables غير محملة  
**الحل:**
1. تأكد من وجود VITE_SUPABASE_URL في frontend/.env
2. أعد تشغيل dev server (Ctrl+C ثم npm run dev)

### ❌ الصفحة تظل في Loading
**السبب:** مشكلة في الاتصال بـ Supabase  
**الحل:**
1. تحقق من Developer Console للأخطاء
2. تأكد من VITE_SUPABASE_ANON_KEY صحيح
3. تحقق من اتصالك بالإنترنت

---

## ✅ معايير النجاح

- [ ] تسجيل الدخول يعمل
- [ ] Dashboard يعرض معلومات المستخدم
- [ ] تسجيل الخروج يعمل
- [ ] ProtectedRoute يحمي Dashboard
- [ ] لا توجد أخطاء في Console

---

**بعد الاختبار، أخبرني بالنتائج!** 🚀
