# دليل إعداد Supabase - خطوة بخطوة

## لماذا Supabase؟

Supabase هي منصة مفتوحة المصدر توفر:
- ✅ PostgreSQL مجاني (500 MB)
- ✅ واجهة سهلة لإدارة قاعدة البيانات
- ✅ SQL Editor مدمج
- ✅ لا حاجة لبطاقة ائتمان
- ✅ دعم ممتاز للمطورين

---

## الخطوة 1: إنشاء حساب على Supabase

1. اذهب إلى: **https://supabase.com**
2. اضغط على **"Start your project"** أو **"Sign in"**
3. اختر **"Sign in with GitHub"**
4. وافق على الصلاحيات المطلوبة

✅ **تم!** الآن لديك حساب على Supabase

---

## الخطوة 2: إنشاء مشروع جديد (Project)

1. بعد تسجيل الدخول، اضغط على **"New Project"**

2. املأ المعلومات التالية:

   - **Name**: `my-tiny-app` (أو أي اسم تريده)
   
   - **Database Password**: أدخل كلمة مرور قوية
     ```
     مثال: MySecurePass123!@#
     ```
     ⚠️ **مهم جداً**: احفظ هذه الكلمة في مكان آمن! ستحتاجها لاحقاً
   
   - **Region**: اختر الأقرب لموقعك
     - `West EU (Ireland)` - للشرق الأوسط وأوروبا
     - `Southeast Asia (Singapore)` - لآسيا
     - `East US (North Virginia)` - لأمريكا
   
   - **Pricing Plan**: اختر **Free** (مجاني)

3. اضغط على **"Create new project"**

4. انتظر 1-2 دقيقة حتى يتم إنشاء المشروع

✅ **تم!** الآن لديك قاعدة بيانات PostgreSQL جاهزة

---

## الخطوة 3: تشغيل ملف init.sql

الآن سنقوم بإنشاء جدول المستخدمين وإضافة مستخدم تجريبي:

### الطريقة السهلة (من واجهة Supabase):

1. في لوحة تحكم المشروع، اذهب إلى **SQL Editor** من القائمة الجانبية

2. اضغط على **"New query"**

3. افتح ملف `backend/db/init.sql` من مشروعك

4. انسخ **كل** محتوى الملف

5. الصق المحتوى في SQL Editor

6. اضغط على **"Run"** (أو اضغط Ctrl+Enter)

7. يجب أن ترى رسالة نجاح: ✅ **"Success. No rows returned"**

### التحقق من نجاح العملية:

1. اذهب إلى **Table Editor** من القائمة الجانبية

2. يجب أن ترى جدول **`users`**

3. اضغط على الجدول لرؤية البيانات

4. يجب أن ترى مستخدم واحد:
   - **username**: `demo`
   - **hashed_password**: (كلمة مرور مشفرة)

✅ **تم!** قاعدة البيانات جاهزة للاستخدام

---

## الخطوة 4: الحصول على Connection String

الآن نحتاج إلى رابط الاتصال بقاعدة البيانات:

1. اذهب إلى **Project Settings** (أيقونة الترس ⚙️ في الأسفل)

2. من القائمة الجانبية، اختر **Database**

3. ابحث عن قسم **"Connection string"**

4. اختر **"URI"** من التبويبات

5. انسخ الرابط الذي يبدأ بـ:
   ```
   postgresql://postgres:[YOUR-PASSWORD]@...
   ```

6. **مهم**: استبدل `[YOUR-PASSWORD]` بكلمة المرور التي أدخلتها في الخطوة 2

   مثال:
   ```
   قبل: postgresql://postgres:[YOUR-PASSWORD]@db.abc123.supabase.co:5432/postgres
   بعد: postgresql://postgres:MySecurePass123!@#@db.abc123.supabase.co:5432/postgres
   ```

✅ **تم!** الآن لديك Connection String جاهز

---

## الخطوة 5: تحديث ملف .env في Backend

1. افتح ملف `backend/.env`

2. حدّث قيمة `DATABASE_URL`:

   ```env
   DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.xxx.supabase.co:5432/postgres
   JWT_SECRET=your_super_secret_key_change_this_in_production
   PORT=5000
   NODE_ENV=development
   ```

3. احفظ الملف

---

## الخطوة 6: اختبار الاتصال

الآن لنتأكد أن كل شيء يعمل:

1. افتح Terminal في مجلد `backend`

2. شغّل الخادم:
   ```bash
   npm start
   ```

3. يجب أن ترى:
   ```
   ✅ Database connected successfully
   🚀 Server running on http://localhost:5000
   ```

4. اختبر endpoint الصحة:
   - افتح المتصفح
   - اذهب إلى: http://localhost:5000/api/health
   - يجب أن ترى:
     ```json
     {
       "status": "ok",
       "database": "connected"
     }
     ```

✅ **مبروك!** قاعدة البيانات تعمل بنجاح

---

## الخطوة 7: اختبار تسجيل الدخول

الآن لنختبر المستخدم التجريبي:

### باستخدام Thunder Client أو Postman:

1. أنشئ طلب POST جديد

2. URL: `http://localhost:5000/api/auth/login`

3. Headers:
   ```
   Content-Type: application/json
   ```

4. Body (JSON):
   ```json
   {
     "username": "demo",
     "password": "demo123"
   }
   ```

5. أرسل الطلب

6. يجب أن تحصل على:
   ```json
   {
     "success": true,
     "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
     "user": {
       "id": 1,
       "username": "demo"
     }
   }
   ```

✅ **ممتاز!** نظام المصادقة يعمل

---

## نصائح مهمة

### 🔒 الأمان:
- **لا تشارك** كلمة مرور قاعدة البيانات مع أحد
- **لا ترفع** ملف `.env` إلى GitHub
- استخدم كلمات مرور قوية دائماً

### 📊 مراقبة الاستخدام:
- اذهب إلى **Reports** في Supabase لمراقبة استخدام قاعدة البيانات
- الخطة المجانية تعطيك 500 MB (كافية لمشروع تعليمي)

### 🛠️ أدوات مفيدة:
- **Table Editor**: لعرض وتعديل البيانات مباشرة
- **SQL Editor**: لتشغيل استعلامات SQL
- **Database Logs**: لمراقبة الأخطاء

---

## استكشاف الأخطاء

### ❌ خطأ: "connection refused"
**الحل**: تأكد من:
- Connection String صحيح
- كلمة المرور صحيحة (بدون أقواس)
- الإنترنت متصل

### ❌ خطأ: "password authentication failed"
**الحل**: 
- تأكد من استبدال `[YOUR-PASSWORD]` بكلمة المرور الفعلية
- تأكد من عدم وجود مسافات زائدة

### ❌ خطأ: "relation users does not exist"
**الحل**:
- ارجع للخطوة 3 وشغّل init.sql مرة أخرى
- تأكد من تشغيل كل الأوامر في SQL Editor

---

## الخطوات التالية

بعد إكمال هذا الإعداد:

1. ✅ قاعدة البيانات جاهزة على Supabase
2. ✅ Backend يتصل بقاعدة البيانات
3. ✅ المستخدم التجريبي جاهز للاختبار

**الآن يمكنك**:
- تشغيل Frontend والاتصال بـ Backend
- نشر Backend على Render أو Railway
- نشر Frontend على Vercel

---

## روابط مفيدة

- 📚 Supabase Docs: https://supabase.com/docs
- 🎥 Supabase YouTube: https://www.youtube.com/c/Supabase
- 💬 Supabase Discord: https://discord.supabase.com

---

**مبروك! 🎉 قاعدة البيانات الخاصة بك جاهزة على Supabase**
