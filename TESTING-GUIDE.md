# دليل اختبار التطبيق محلياً 🧪

هذا الدليل يشرح كيفية اختبار التطبيق الكامل على جهازك المحلي.

---

## ✅ التطبيق يعمل الآن!

### Backend:
- 🟢 **يعمل على**: http://localhost:5000
- ✅ قاعدة البيانات متصلة
- ✅ جميع APIs جاهزة

### Frontend:
- 🟢 **يعمل على**: http://localhost:5173
- ✅ React + Vite يعمل
- ✅ متصل بـ Backend

---

## 🧪 سيناريوهات الاختبار

### 1. اختبار الصفحة الرئيسية

**الخطوات**:
1. افتح المتصفح على: http://localhost:5173
2. يجب أن ترى الصفحة الرئيسية

**ما يجب أن تراه**:
- ✅ عنوان "مشروع تعليمي للنشر والاستضافة"
- ✅ بطاقات التقنيات (6 بطاقات)
- ✅ قائمة الميزات
- ✅ زر "تسجيل الدخول"
- ✅ بيانات المستخدم التجريبي (demo/demo123)

---

### 2. اختبار تسجيل الدخول

**الخطوات**:
1. من الصفحة الرئيسية، اضغط "تسجيل الدخول"
2. أو اذهب مباشرة إلى: http://localhost:5173/login
3. أدخل:
   - اسم المستخدم: `demo`
   - كلمة المرور: `demo123`
4. اضغط "تسجيل الدخول"

**ما يجب أن يحدث**:
- ✅ عرض رسالة "تم تسجيل الدخول بنجاح"
- ✅ إعادة توجيه تلقائية إلى Dashboard
- ✅ حفظ Token في localStorage

**اختبار الأخطاء**:
- جرب كلمة مرور خاطئة → يجب أن ترى رسالة خطأ
- اترك الحقول فارغة → يجب أن ترى رسالة "يرجى إدخال..."

---

### 3. اختبار Dashboard

**الخطوات**:
1. بعد تسجيل الدخول، يجب أن تكون في Dashboard
2. أو اذهب إلى: http://localhost:5173/dashboard

**ما يجب أن تراه**:
- ✅ رسالة ترحيب "مرحباً demo!"
- ✅ بطاقة معلومات المستخدم:
  - اسم المستخدم: demo
  - معرف المستخدم: #1
  - تاريخ التسجيل
  - عدد الأيام
- ✅ بطاقة الإحصائيات:
  - عدد تسجيلات الدخول
  - آخر تسجيل دخول
- ✅ بطاقة الترحيب مع الميزات
- ✅ بطاقة الإجراءات مع أزرار
- ✅ زر "تسجيل الخروج" في الأعلى

---

### 4. اختبار الحماية (Protected Route)

**الخطوات**:
1. افتح نافذة تصفح خفي (Incognito)
2. اذهب مباشرة إلى: http://localhost:5173/dashboard

**ما يجب أن يحدث**:
- ✅ إعادة توجيه تلقائية إلى صفحة تسجيل الدخول
- ✅ لا يمكن الوصول إلى Dashboard بدون تسجيل دخول

---

### 5. اختبار تسجيل الخروج

**الخطوات**:
1. من Dashboard، اضغط زر "تسجيل الخروج"

**ما يجب أن يحدث**:
- ✅ حذف Token من localStorage
- ✅ إعادة توجيه إلى الصفحة الرئيسية
- ✅ لا يمكن الوصول إلى Dashboard بعد الخروج

---

### 6. اختبار تحديث البيانات

**الخطوات**:
1. في Dashboard، اضغط زر "تحديث البيانات"

**ما يجب أن يحدث**:
- ✅ عرض Loading spinner
- ✅ جلب البيانات من Backend
- ✅ تحديث البيانات على الشاشة

---

### 7. اختبار Backend APIs

**باستخدام المتصفح**:

1. **Health Check**:
   - افتح: http://localhost:5000/api/health
   - يجب أن ترى: `{"status":"ok","database":"connected"}`

2. **الصفحة الرئيسية**:
   - افتح: http://localhost:5000
   - يجب أن ترى معلومات عن API

**باستخدام curl** (في Terminal):

```bash
# Health Check
curl http://localhost:5000/api/health

# تسجيل الدخول
curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d "{\"username\":\"demo\",\"password\":\"demo123\"}"

# الحصول على معلومات المستخدم (استبدل TOKEN بالـ token الحقيقي)
curl http://localhost:5000/api/user/profile \
  -H "Authorization: Bearer TOKEN"
```

---

## 🔍 فحص Developer Tools

### في المتصفح:

1. اضغط `F12` لفتح Developer Tools
2. اذهب إلى تبويب **Console**
3. يجب ألا ترى أخطاء (errors)

### فحص Network:

1. في Developer Tools، اذهب إلى **Network**
2. سجل دخول
3. يجب أن ترى:
   - طلب POST إلى `/api/auth/login`
   - Status: 200
   - Response يحتوي على token

### فحص localStorage:

1. في Developer Tools، اذهب إلى **Application** → **Local Storage**
2. بعد تسجيل الدخول، يجب أن ترى:
   - Key: `token`
   - Value: JWT token طويل

---

## ✅ قائمة التحقق الكاملة

### Backend:
- [ ] الخادم يعمل على http://localhost:5000
- [ ] قاعدة البيانات متصلة
- [ ] `/api/health` يعمل
- [ ] `/api/auth/login` يعمل
- [ ] `/api/user/profile` يعمل (مع token)
- [ ] `/api/user/dashboard` يعمل (مع token)

### Frontend:
- [ ] التطبيق يعمل على http://localhost:5173
- [ ] الصفحة الرئيسية تظهر بشكل صحيح
- [ ] صفحة تسجيل الدخول تعمل
- [ ] تسجيل الدخول ينجح مع بيانات صحيحة
- [ ] تسجيل الدخول يفشل مع بيانات خاطئة
- [ ] Dashboard يظهر بعد تسجيل الدخول
- [ ] Dashboard محمي (لا يمكن الوصول بدون token)
- [ ] تسجيل الخروج يعمل
- [ ] لا توجد أخطاء في Console

### التكامل:
- [ ] Frontend يتصل بـ Backend بنجاح
- [ ] Token يُحفظ في localStorage
- [ ] Token يُرسل في Headers
- [ ] البيانات تُجلب من Backend
- [ ] الأخطاء تُعالج بشكل صحيح

---

## 🐛 حل المشاكل

### المشكلة: Frontend لا يتصل بـ Backend

**الحل**:
1. تأكد من تشغيل Backend على المنفذ 5000
2. تحقق من `VITE_API_URL` في `frontend/.env`
3. تأكد من إعدادات CORS في Backend

### المشكلة: "Network Error"

**الحل**:
1. تأكد من تشغيل Backend
2. افتح http://localhost:5000/api/health في المتصفح
3. إذا لم يعمل، أعد تشغيل Backend

### المشكلة: "Token منتهي الصلاحية"

**الحل**:
1. سجل خروج
2. سجل دخول مرة أخرى
3. Token صالح لمدة 7 أيام

### المشكلة: Dashboard لا يعرض البيانات

**الحل**:
1. افتح Developer Tools → Console
2. ابحث عن أخطاء
3. تحقق من Network → XHR
4. تأكد من وجود Token في localStorage

---

## 📊 النتيجة المتوقعة

إذا نجحت جميع الاختبارات:

✅ **Backend**: يعمل بشكل كامل
✅ **Frontend**: يعمل بشكل كامل
✅ **التكامل**: يعمل بشكل كامل
✅ **المصادقة**: تعمل بشكل صحيح
✅ **الحماية**: تعمل بشكل صحيح

**مبروك! التطبيق جاهز للنشر! 🎉**

---

## 🚀 الخطوة التالية

بعد نجاح الاختبار المحلي:
- ✅ المهمة 12: اختبار التطبيق محلياً - **مكتملة**
- ⏭️ المهمة 13: إعداد GitHub Repository
- ⏭️ المهمة 14-21: النشر على Render و Vercel
