# استكشاف أخطاء Supabase 🔧

## ❌ المشكلة الحالية

```
خطأ في الاتصال بقاعدة البيانات: getaddrinfo ENOENT db.uprbqxwyrduvjflxkrkf.supabase.co
```

---

## 🔍 تشخيص المشكلة

هذا الخطأ يعني أن النظام **لا يستطيع إيجاد عنوان الخادم**.

### الأسباب المحتملة:

1. ❌ **عنوان الخادم غير صحيح**
   - Connection String منسوخ بشكل خاطئ
   - المشروع تم حذفه من Supabase
   - اسم المشروع خاطئ

2. ❌ **مشكلة في الإنترنت**
   - الاتصال بالإنترنت غير مستقر
   - Firewall يمنع الاتصال

3. ❌ **المشروع غير نشط**
   - المشروع في حالة Paused
   - المشروع لم يكتمل إنشاؤه

---

## ✅ الحلول

### الحل 1: التحقق من Connection String

1. **اذهب إلى Supabase Dashboard**
   ```
   https://supabase.com/dashboard
   ```

2. **افتح مشروعك** (my-tiny-app أو أي اسم آخر)

3. **اذهب إلى Project Settings** (أيقونة الترس ⚙️)

4. **اختر Database** من القائمة الجانبية

5. **ابحث عن Connection string**
   - اختر تبويب **"URI"**
   - انسخ الرابط كاملاً

6. **تأكد من الرابط يبدأ بـ:**
   ```
   postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres
   ```

7. **استبدل `[YOUR-PASSWORD]`** بكلمة المرور التي أدخلتها عند إنشاء المشروع

---

### الحل 2: التحقق من حالة المشروع

1. في Supabase Dashboard، تأكد من:
   - ✅ المشروع في حالة **"Active"** (أخضر)
   - ✅ ليس في حالة **"Paused"** (رمادي)
   - ✅ ليس في حالة **"Restoring"** (أصفر)

2. إذا كان المشروع Paused:
   - اضغط على **"Restore"** أو **"Resume"**
   - انتظر حتى يصبح Active

---

### الحل 3: إنشاء مشروع جديد

إذا لم تجد المشروع أو كان محذوفاً:

1. **أنشئ مشروع جديد:**
   ```
   https://supabase.com/dashboard → New Project
   ```

2. **املأ المعلومات:**
   - Name: `my-tiny-app-v2`
   - Password: كلمة مرور قوية (احفظها!)
   - Region: اختر الأقرب لك
   - Plan: Free

3. **انتظر 1-2 دقيقة** حتى يكتمل الإنشاء

4. **شغّل init.sql:**
   - SQL Editor → New query
   - انسخ محتوى `backend/db/init.sql`
   - Run

5. **احصل على Connection String الجديد**

---

### الحل 4: اختبار الاتصال بالإنترنت

```bash
# اختبر الاتصال بـ Supabase
ping supabase.com

# إذا لم يعمل، تحقق من:
# - الاتصال بالإنترنت
# - إعدادات Firewall
# - VPN (إذا كنت تستخدم واحد)
```

---

## 📝 خطوات التحقق

### 1. تحقق من ملف .env

افتح `backend/.env` وتأكد من:

```env
DATABASE_URL=postgresql://postgres:YOUR_ACTUAL_PASSWORD@db.xxxxx.supabase.co:5432/postgres
```

**تأكد من:**
- ✅ لا توجد مسافات زائدة
- ✅ كلمة المرور صحيحة (بدون أقواس [])
- ✅ عنوان الخادم صحيح (db.xxxxx.supabase.co)
- ✅ لا توجد أحرف خاصة غير مشفرة في كلمة المرور

---

### 2. اختبر Connection String

يمكنك اختبار Connection String مباشرة:

```bash
# في مجلد backend
node -e "const {Pool} = require('pg'); const pool = new Pool({connectionString: process.env.DATABASE_URL}); pool.query('SELECT NOW()').then(r => console.log('✅ متصل:', r.rows[0])).catch(e => console.log('❌ خطأ:', e.message))"
```

---

### 3. تحقق من Supabase Dashboard

1. اذهب إلى: https://supabase.com/dashboard
2. تحقق من وجود المشروع
3. تحقق من حالة المشروع (Active)
4. تحقق من Connection String

---

## 🔧 حل سريع

إذا كنت متأكداً من أن المشروع موجود:

### الخطوة 1: احصل على Connection String الصحيح

```
Supabase Dashboard → Project Settings → Database → Connection string (URI)
```

### الخطوة 2: حدّث backend/.env

```env
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.xxxxx.supabase.co:5432/postgres
```

### الخطوة 3: اختبر مرة أخرى

```bash
cd backend
npm start
```

يجب أن ترى:
```
✅ تم الاتصال بقاعدة البيانات بنجاح!
```

---

## 💡 نصائح

### كلمات المرور مع أحرف خاصة

إذا كانت كلمة المرور تحتوي على أحرف خاصة مثل `@`, `#`, `&`:

**استخدم URL encoding:**
- `@` → `%40`
- `#` → `%23`
- `&` → `%26`
- `!` → `%21`

**مثال:**
```
كلمة المرور: Pass@123!
في Connection String: Pass%40123%21
```

---

## 📞 الحصول على المساعدة

إذا استمرت المشكلة:

1. **تحقق من Supabase Status:**
   ```
   https://status.supabase.com
   ```

2. **راجع Supabase Docs:**
   ```
   https://supabase.com/docs/guides/database/connecting-to-postgres
   ```

3. **Supabase Discord:**
   ```
   https://discord.supabase.com
   ```

---

## ✅ الخطوات التالية

بعد حل المشكلة:

1. ✅ تأكد من الاتصال بنجاح
2. ✅ اختبر `/api/health` endpoint
3. ✅ اختبر تسجيل الدخول
4. ✅ انتقل للمهمة 15 (نشر على Render)

---

**تحديث:** 19 نوفمبر 2025  
**الحالة:** يحتاج إلى التحقق من Connection String
