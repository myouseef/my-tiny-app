# خطة التنفيذ - مشروع تعليمي للنشر والاستضافة

## نظرة عامة
هذه الخطة تحتوي على خطوات تنفيذ المشروع بالترتيب. كل خطوة تبني على الخطوة السابقة، وفي النهاية سيكون لديك تطبيق كامل منشور على الإنترنت.

---

## المهام

- [x] 1. إعداد قاعدة البيانات PostgreSQL



  - إنشاء جدول المستخدمين (users table) مع الحقول المطلوبة (id, username, hashed_password, created_at)
  - إضافة قيد unique على حقل username
  - إنشاء مستخدم تجريبي للاختبار (username: demo, password: demo123)
  - كتابة ملف SQL للإعداد الأولي في `backend/db/init.sql`
  - _المتطلبات: 4.1, 4.2, 4.5_





- [x] 2. بناء Backend API - الإعداد الأساسي

  - تثبيت المكتبات المطلوبة: express, pg, bcrypt, jsonwebtoken, cors, dotenv
  - إنشاء ملف `backend/server.js` الرئيسي



  - إعداد Express server مع CORS middleware
  - إنشاء اتصال بقاعدة البيانات PostgreSQL باستخدام متغيرات البيئة
  - إضافة endpoint للتحقق من صحة الخادم (GET /api/health)
  - _المتطلبات: 1.2, 4.3, 4.4_

- [x] 3. بناء نظام المصادقة في Backend

  - إنشاء ملف `backend/routes/auth.js` لمسارات المصادقة
  - تنفيذ endpoint تسجيل الدخول (POST /api/auth/login)
  - التحقق من بيانات المستخدم في قاعدة البيانات
  - مقارنة كلمة المرور المدخلة مع المشفرة باستخدام bcrypt
  - إنشاء JWT token عند نجاح تسجيل الدخول
  - إرجاع استجابة JSON مع token ومعلومات المستخدم
  - معالجة الأخطاء (بيانات خاطئة، مستخدم غير موجود)
  - _المتطلبات: 3.2, 3.3, 4.2_

- [x] 4. بناء Middleware للحماية



  - إنشاء ملف `backend/middleware/auth.js`
  - تنفيذ middleware للتحقق من JWT token
  - استخراج token من header (Authorization: Bearer token)
  - التحقق من صحة token باستخدام JWT_SECRET
  - إضافة معلومات المستخدم إلى request object
  - معالجة الأخطاء (token منتهي، token غير صحيح، token مفقود)
  - _المتطلبات: 3.6_

- [x] 5. بناء Protected Endpoints



  - إنشاء ملف `backend/routes/user.js`
  - تنفيذ endpoint للحصول على معلومات المستخدم (GET /api/user/profile)
  - تطبيق auth middleware على المسار المحمي
  - إرجاع معلومات المستخدم من قاعدة البيانات
  - _المتطلبات: 3.6_

- [x] 6. إعداد Frontend - البنية الأساسية



  - التأكد من تثبيت المكتبات: react, react-dom, react-router-dom
  - إنشاء ملف `frontend/src/App.jsx` الرئيسي
  - إعداد React Router مع المسارات الأساسية (/, /login, /dashboard)
  - إنشاء ملف `frontend/src/services/api.js` للتعامل مع API calls
  - إضافة دالة مساعدة للتحقق من وجود token في localStorage
  - _المتطلبات: 1.1, 2.5_

- [x] 7. بناء الصفحة الرئيسية (Home Page)



  - إنشاء مكون `frontend/src/components/HomePage.jsx`
  - عرض رسالة ترحيب وشرح بسيط عن المشروع
  - إضافة رابط للانتقال إلى صفحة تسجيل الدخول
  - تطبيق تنسيق CSS بسيط
  - _المتطلبات: 2.1_

- [x] 8. بناء صفحة تسجيل الدخول (Login Page)



  - إنشاء مكون `frontend/src/components/LoginPage.jsx`
  - إنشاء نموذج (form) مع حقول username و password
  - إضافة state management لحقول الإدخال
  - تنفيذ دالة handleSubmit لإرسال بيانات تسجيل الدخول إلى API
  - حفظ JWT token في localStorage عند النجاح
  - عرض رسائل الخطأ عند فشل تسجيل الدخول
  - إعادة التوجيه إلى /dashboard بعد نجاح تسجيل الدخول
  - إضافة loading state أثناء الطلب
  - _المتطلبات: 2.2, 3.1, 3.2, 3.3, 3.4, 3.5_

- [x] 9. بناء مكون الحماية (Protected Route)

  - إنشاء مكون `frontend/src/components/ProtectedRoute.jsx`
  - التحقق من وجود token في localStorage
  - إعادة التوجيه إلى /login إذا لم يكن المستخدم مسجل دخول
  - السماح بعرض المكون المحمي إذا كان token موجود
  - _المتطلبات: 2.4_

- [x] 10. بناء صفحة Dashboard المحمية



  - إنشاء مكون `frontend/src/components/DashboardPage.jsx`
  - استدعاء API للحصول على معلومات المستخدم (GET /api/user/profile)
  - إرسال JWT token في header (Authorization: Bearer token)
  - عرض معلومات المستخدم (username)
  - إضافة زر تسجيل الخروج (Logout)
  - تنفيذ دالة logout لحذف token من localStorage
  - إعادة التوجيه إلى / بعد تسجيل الخروج
  - معالجة حالة التحميل والأخطاء
  - _المتطلبات: 2.3, 2.4_

- [x] 11. إعداد ملفات Environment Variables



  - إنشاء ملف `backend/.env` مع المتغيرات المطلوبة (DATABASE_URL, JWT_SECRET, PORT)
  - إنشاء ملف `frontend/.env` مع VITE_API_URL
  - إضافة ملفات .env إلى .gitignore
  - إنشاء ملفات .env.example كنماذج للمطورين
  - _المتطلبات: 4.4_

- [x] 12. اختبار التطبيق محلياً



  - تشغيل PostgreSQL محلياً وتنفيذ init.sql
  - تشغيل Backend server (npm start في مجلد backend)
  - تشغيل Frontend dev server (npm run dev في مجلد frontend)
  - اختبار تسجيل الدخول بالمستخدم التجريبي (demo/demo123)
  - اختبار الوصول إلى Dashboard
  - اختبار تسجيل الخروج
  - اختبار محاولة الوصول إلى Dashboard بدون تسجيل دخول
  - التحقق من عمل جميع الوظائف بشكل صحيح
  - _المتطلبات: 3.1, 3.2, 3.3, 3.5, 2.4_

- [-] 13. إعداد GitHub Repository

  - إنشاء مستودع جديد على GitHub
  - إنشاء ملف .gitignore شامل (node_modules, .env, dist, build)
  - إنشاء ملف README.md مع تعليمات الإعداد والتشغيل
  - إضافة شرح بالعربية لكل أداة مستخدمة في README
  - عمل commit أولي ورفع الكود إلى GitHub
  - _المتطلبات: 7.1, 7.2, 7.3, 8.1, 8.2_

- [ ] 14. نشر قاعدة البيانات على Render
  - إنشاء حساب على Render.com
  - إنشاء PostgreSQL database جديدة (خطة مجانية)
  - نسخ Internal Database URL
  - الاتصال بقاعدة البيانات وتنفيذ init.sql لإنشاء الجداول
  - التحقق من إنشاء جدول users والمستخدم التجريبي
  - _المتطلبات: 6.2_

- [ ] 15. نشر Backend على Render
  - إنشاء Web Service جديد على Render
  - ربط مستودع GitHub
  - تحديد مجلد backend كـ Root Directory
  - إعداد Build Command: `npm install`
  - إعداد Start Command: `node server.js`
  - إضافة Environment Variables (DATABASE_URL, JWT_SECRET, NODE_ENV=production)
  - نشر الخدمة والانتظار حتى تصبح live
  - نسخ رابط API العام
  - اختبار API باستخدام endpoint /api/health
  - _المتطلبات: 6.1, 6.3, 6.4, 6.5_

- [ ] 16. نشر Frontend على Vercel
  - إنشاء حساب على Vercel.com
  - إنشاء مشروع جديد وربط مستودع GitHub
  - تحديد مجلد frontend كـ Root Directory
  - إعداد Framework Preset: Vite
  - إضافة Environment Variable: VITE_API_URL (رابط Render API)
  - نشر المشروع والانتظار حتى يكتمل البناء
  - نسخ رابط التطبيق العام
  - _المتطلبات: 5.1, 5.2, 5.4, 5.5_

- [ ] 17. إعداد ملف vercel.json للتوجيه
  - إنشاء ملف `frontend/vercel.json`
  - إضافة قاعدة rewrites لدعم React Router
  - عمل commit و push للتحديث
  - التحقق من إعادة النشر التلقائي على Vercel
  - _المتطلبات: 5.3_

- [ ] 18. تحديث CORS في Backend للإنتاج
  - تحديث إعدادات CORS في backend/server.js
  - إضافة رابط Vercel إلى allowed origins
  - استخدام متغير بيئة FRONTEND_URL
  - عمل commit و push
  - التحقق من إعادة النشر التلقائي على Render
  - _المتطلبات: 6.4_

- [ ] 19. اختبار التطبيق المنشور
  - فتح رابط Vercel في المتصفح
  - اختبار تسجيل الدخول بالمستخدم التجريبي
  - التحقق من عمل Dashboard
  - اختبار تسجيل الخروج
  - اختبار محاولة الوصول المباشر إلى /dashboard بدون تسجيل دخول
  - فتح Developer Console والتحقق من عدم وجود أخطاء
  - اختبار من أجهزة مختلفة (موبايل، تابلت)
  - _المتطلبات: 3.1, 3.2, 3.3, 3.5, 2.4_

- [ ] 20. إعداد Auto-Deployment
  - التحقق من ربط GitHub مع Vercel (auto-deploy on push)
  - التحقق من ربط GitHub مع Render (auto-deploy on push)
  - عمل تغيير بسيط في الكود (مثل تغيير نص الترحيب)
  - عمل commit و push
  - مراقبة عملية البناء والنشر التلقائي على كلا المنصتين
  - التحقق من ظهور التغيير على الموقع المنشور
  - _المتطلبات: 7.4, 5.2, 6.3_

- [ ] 21. كتابة التوثيق النهائي
  - تحديث README.md مع معلومات النشر
  - إضافة روابط التطبيق المنشور (Vercel URL)
  - إضافة لقطات شاشة (screenshots) للتطبيق
  - كتابة قسم "ما تعلمته" بالعربية
  - إضافة قسم "المشاكل التي واجهتها وحلولها"
  - إضافة قسم "الخطوات التالية" للتطوير المستقبلي
  - توثيق متغيرات البيئة المطلوبة
  - إضافة روابط للموارد التعليمية المفيدة
  - _المتطلبات: 8.1, 8.2, 8.3, 8.4, 8.5_

---

## ملاحظات مهمة

### عن المهام الاختيارية
بعض المهام الفرعية تم وضع علامة * عليها لأنها اختيارية. هذا المشروع يركز على التعلم والتجربة، لذلك لم نضف مهام اختبار وحدة (unit tests) معقدة. الاختبار اليدوي كافٍ لهذا المشروع التعليمي.

### ترتيب التنفيذ
من المهم جداً اتباع الترتيب المذكور:
1. ابدأ بقاعدة البيانات والـ Backend
2. ثم Frontend
3. ثم الاختبار المحلي
4. وأخيراً النشر

### نصائح للنجاح
- اختبر كل خطوة قبل الانتقال للتالية
- احفظ نسخة من environment variables في مكان آمن
- اقرأ رسائل الأخطاء بعناية - غالباً تحتوي على الحل
- استخدم console.log للتتبع أثناء التطوير
- لا تنسَ عمل commit بعد كل خطوة مهمة

### موارد مساعدة
- إذا واجهت مشكلة في Vercel: راجع Deployment Logs
- إذا واجهت مشكلة في Render: راجع Service Logs
- إذا واجهت مشكلة في CORS: تأكد من إعدادات CORS في Backend
- إذا واجهت مشكلة في Database: تحقق من DATABASE_URL

---

## بعد إكمال جميع المهام

ستكون قد أنجزت:
✅ تطبيق ويب كامل (Full-Stack) يعمل على الإنترنت
✅ تعلمت React, Vite, Express, PostgreSQL
✅ تعلمت كيفية النشر على Vercel و Render
✅ تعلمت Git و GitHub و CI/CD
✅ فهمت كيفية عمل JWT Authentication
✅ لديك مشروع يمكنك إضافته لـ Portfolio الخاص بك

**مبروك مقدماً! 🎉**
