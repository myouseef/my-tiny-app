# دليل حل المشاكل (Troubleshooting) 🔧

هذا الدليل يساعدك في حل المشاكل الشائعة التي قد تواجهها.

---

## ❌ مشكلة: "password authentication failed for user postgres"

### الأعراض:
```
❌ خطأ في الاتصال بقاعدة البيانات: password authentication failed for user "postgres"
```

### السبب:
كلمة مرور PostgreSQL في ملف `.env` غير صحيحة.

### الحل:

#### الخيار 1: تحديث كلمة المرور في .env

1. افتح ملف `backend/.env`
2. عدّل سطر `DATABASE_URL`:
```env
DATABASE_URL=postgresql://postgres:YOUR_ACTUAL_PASSWORD@localhost:5432/learning_db
```
3. استبدل `YOUR_ACTUAL_PASSWORD` بكلمة المرور الصحيحة

#### الخيار 2: إعادة تعيين كلمة مرور PostgreSQL

**Windows:**
```bash
# افتح SQL Shell (psql)
# اضغط Enter عدة مرات حتى تصل لكلمة المرور
# أدخل كلمة المرور الحالية

# ثم نفذ:
ALTER USER postgres PASSWORD 'new_password';
```

**macOS/Linux:**
```bash
sudo -u postgres psql
ALTER USER postgres PASSWORD 'new_password';
\q
```

#### الخيار 3: استخدام مستخدم مختلف

إذا كنت تستخدم مستخدم PostgreSQL آخر:
```env
DATABASE_URL=postgresql://your_username:your_password@localhost:5432/learning_db
```

---

## ❌ مشكلة: "database does not exist"

### الأعراض:
```
database "learning_db" does not exist
```

### الحل:

```bash
# افتح psql
psql -U postgres

# أنشئ قاعدة البيانات
CREATE DATABASE learning_db;

# اخرج
\q

# شغّل سكريبت الإعداد
cd backend
npm run db:init
```

---

## ❌ مشكلة: "relation users does not exist"

### الأعراض:
```
relation "users" does not exist
```

### السبب:
جدول المستخدمين غير موجود في قاعدة البيانات.

### الحل:

```bash
cd backend
npm run db:init
```

أو يدوياً:
```bash
psql -U postgres -d learning_db -f db/init.sql
```

---

## ❌ مشكلة: "EADDRINUSE: address already in use"

### الأعراض:
```
Error: listen EADDRINUSE: address already in use :::5000
```

### السبب:
المنفذ 5000 مستخدم بالفعل من عملية أخرى.

### الحل:

#### الخيار 1: إيقاف العملية الأخرى

**Windows:**
```bash
# ابحث عن العملية
netstat -ano | findstr :5000

# أوقف العملية (استبدل PID برقم العملية)
taskkill /PID <PID> /F
```

**macOS/Linux:**
```bash
# ابحث عن العملية
lsof -i :5000

# أوقف العملية
kill -9 <PID>
```

#### الخيار 2: تغيير المنفذ

عدّل ملف `.env`:
```env
PORT=5001
```

---

## ❌ مشكلة: "Cannot find module"

### الأعراض:
```
Error: Cannot find module 'express'
```

### الحل:

```bash
cd backend
npm install
```

---

## ❌ مشكلة: "JWT_SECRET is not defined"

### الأعراض:
```
Error: JWT_SECRET is not defined
```

### الحل:

تأكد من وجود `JWT_SECRET` في ملف `.env`:
```env
JWT_SECRET=your_super_secret_key_here
```

---

## ✅ اختبار سريع للتأكد من عمل كل شيء

### 1. اختبار قاعدة البيانات:
```bash
cd backend
npm run db:test
```

يجب أن ترى:
```
✅ تم الاتصال بقاعدة البيانات بنجاح!
✅ جدول المستخدمين موجود
👥 عدد المستخدمين في قاعدة البيانات: 1
```

### 2. اختبار الخادم:
```bash
npm start
```

يجب أن ترى:
```
🚀 الخادم يعمل الآن!
✅ تم الاتصال بقاعدة البيانات بنجاح!
```

### 3. اختبار تسجيل الدخول:
```bash
# في terminal آخر
node test-login.js
```

يجب أن ترى:
```
✅ تم تسجيل الدخول بنجاح!
```

---

## 📝 ملاحظات مهمة

1. **تأكد من تشغيل PostgreSQL** قبل تشغيل الخادم
2. **لا ترفع ملف .env** على GitHub
3. **استخدم كلمات مرور قوية** في الإنتاج
4. **راجع سجلات الأخطاء** في Console للمزيد من التفاصيل

---

## 🆘 لا تزال المشكلة موجودة؟

1. تأكد من تثبيت جميع المتطلبات:
   - Node.js v18+
   - PostgreSQL v14+
   - جميع المكتبات (`npm install`)

2. راجع ملفات التوثيق:
   - `backend/README.md`
   - `backend/db/SETUP-GUIDE.md`
   - `backend/routes/AUTH-API.md`

3. تحقق من:
   - ملف `.env` موجود وصحيح
   - PostgreSQL يعمل
   - قاعدة البيانات `learning_db` موجودة
   - جدول `users` موجود

---

**نصيحة**: معظم المشاكل تكون بسبب إعدادات قاعدة البيانات. ابدأ بالتحقق من DATABASE_URL أولاً! 💡
