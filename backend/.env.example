# ملف نموذجي لمتغيرات البيئة
# انسخ هذا الملف إلى .env وعدّل القيم حسب إعداداتك

# ===== قاعدة البيانات (Database) =====
# رابط الاتصال بقاعدة البيانات PostgreSQL
# الصيغة: postgresql://username:password@host:port/database_name
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/learning_db

# ===== الأمان (Security) =====
# مفتاح سري لتشفير JWT tokens
# استخدم مفتاح عشوائي طويل ومعقد
# يمكنك توليد مفتاح باستخدام: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_key_change_this_in_production

# ===== الخادم (Server) =====
# رقم المنفذ الذي سيعمل عليه الخادم
PORT=5000

# بيئة التشغيل (development, production, test)
NODE_ENV=development

# ===== Frontend =====
# رابط تطبيق Frontend (للـ CORS)
# في التطوير المحلي (Vite):
FRONTEND_URL=http://localhost:5173
# في الإنتاج (بعد النشر على Vercel):
# FRONTEND_URL=https://your-app.vercel.app
