# Frontend - دليل الاستخدام ⚛️

هذا هو التطبيق الأمامي (Frontend) للمشروع التعليمي، مبني باستخدام React + Vite.

## 📋 المحتويات

- [التقنيات المستخدمة](#التقنيات-المستخدمة)
- [هيكل المشروع](#هيكل-المشروع)
- [التثبيت والإعداد](#التثبيت-والإعداد)
- [التشغيل](#التشغيل)
- [المكونات](#المكونات)
- [الصفحات](#الصفحات)

---

## التقنيات المستخدمة

### React 19
**ما هي؟** مكتبة JavaScript لبناء واجهات المستخدم

**لماذا نستخدمها؟**
- مكونات قابلة لإعادة الاستخدام
- Virtual DOM للأداء العالي
- مجتمع كبير ومكتبات كثيرة
- سهلة التعلم

### Vite
**ما هي؟** أداة بناء حديثة وسريعة جداً

**لماذا نستخدمها؟**
- سرعة فائقة في التطوير (Hot Module Replacement)
- بناء محسّن للإنتاج
- إعداد بسيط وسهل
- دعم ممتاز لـ React

### React Router DOM
**ما هي؟** مكتبة للتنقل بين الصفحات (Routing)

**لماذا نستخدمها؟**
- تنقل سلس بدون إعادة تحميل الصفحة
- حماية المسارات (Protected Routes)
- إدارة سهلة للـ URLs

---

## هيكل المشروع

```
frontend/
├── public/              # ملفات عامة (صور، أيقونات)
│   └── vite.svg
│
├── src/                 # الكود المصدري
│   ├── assets/         # الصور والملفات الثابتة
│   │
│   ├── components/     # المكونات القابلة لإعادة الاستخدام
│   │   └── ProtectedRoute.jsx  # مكون حماية المسارات ✅
│   │
│   ├── pages/          # صفحات التطبيق
│   │   ├── HomePage.jsx        # الصفحة الرئيسية (قريباً)
│   │   ├── LoginPage.jsx       # صفحة تسجيل الدخول (قريباً)
│   │   └── DashboardPage.jsx   # صفحة Dashboard (قريباً)
│   │
│   ├── services/       # خدمات API
│   │   └── api.js      # دوال التواصل مع Backend ✅
│   │
│   ├── App.jsx         # المكون الرئيسي مع Router ✅
│   ├── App.css         # تنسيقات التطبيق
│   ├── main.jsx        # نقطة الدخول
│   └── index.css       # تنسيقات عامة
│
├── .env                # متغيرات البيئة ✅
├── .env.example        # نموذج متغيرات البيئة ✅
├── index.html          # ملف HTML الرئيسي
├── package.json        # معلومات المشروع
├── vite.config.js      # إعدادات Vite
└── README.md           # هذا الملف
```

---

## التثبيت والإعداد

### 1. تثبيت المكتبات

```bash
cd frontend
npm install
```

### 2. إعداد ملف .env

انسخ ملف `.env.example` إلى `.env`:

```bash
copy .env.example .env
```

عدّل ملف `.env`:

```env
VITE_API_URL=http://localhost:5000
```

**ملاحظة**: في Vite، يجب أن تبدأ المتغيرات بـ `VITE_`

---

## التشغيل

### تشغيل خادم التطوير

```bash
npm run dev
```

سيعمل التطبيق على: http://localhost:3000

### بناء للإنتاج

```bash
npm run build
```

سيتم إنشاء مجلد `dist` يحتوي على الملفات المحسّنة.

### معاينة البناء

```bash
npm run preview
```

---

## المكونات

### 1. ProtectedRoute ✅

**الموقع**: `src/components/ProtectedRoute.jsx`

**الوظيفة**: حماية المسارات التي تتطلب تسجيل دخول

**كيفية الاستخدام**:
```jsx
<Route
  path="/dashboard"
  element={
    <ProtectedRoute>
      <DashboardPage />
    </ProtectedRoute>
  }
/>
```

**كيف يعمل**:
1. يتحقق من وجود Token في localStorage
2. إذا كان موجود: يعرض الصفحة
3. إذا لم يكن موجود: يعيد التوجيه لـ /login

---

## الصفحات

### 1. HomePage (قريباً)

**المسار**: `/`

**الوصف**: الصفحة الرئيسية للتطبيق

**المحتوى**:
- رسالة ترحيب
- شرح بسيط عن المشروع
- رابط لصفحة تسجيل الدخول

### 2. LoginPage (قريباً)

**المسار**: `/login`

**الوصف**: صفحة تسجيل الدخول

**المحتوى**:
- نموذج تسجيل الدخول (username, password)
- زر تسجيل الدخول
- رسائل الخطأ
- إعادة توجيه تلقائية بعد النجاح

### 3. DashboardPage (قريباً)

**المسار**: `/dashboard` (محمي)

**الوصف**: صفحة Dashboard للمستخدم

**المحتوى**:
- معلومات المستخدم
- إحصائيات
- زر تسجيل الخروج

---

## خدمات API

### ملف api.js ✅

**الموقع**: `src/services/api.js`

**الوظيفة**: التواصل مع Backend API

**الدوال المتاحة**:

#### دوال المصادقة:
```javascript
import { login, logout, isAuthenticated } from './services/api';

// تسجيل الدخول
const response = await login('demo', 'demo123');
if (response.ok) {
  console.log('تم تسجيل الدخول!');
}

// تسجيل الخروج
await logout();

// التحقق من تسجيل الدخول
if (isAuthenticated()) {
  console.log('مسجل دخول');
}
```

#### دوال المستخدم:
```javascript
import { getUserProfile, getDashboard } from './services/api';

// الحصول على معلومات المستخدم
const profile = await getUserProfile();
console.log(profile.data.user);

// الحصول على Dashboard
const dashboard = await getDashboard();
console.log(dashboard.data.dashboard);
```

#### دوال Token:
```javascript
import { getToken, setToken, removeToken } from './services/api';

// الحصول على Token
const token = getToken();

// حفظ Token
setToken('your_token_here');

// حذف Token
removeToken();
```

---

## React Router

### المسارات المتاحة:

```javascript
/ → HomePage (عام)
/login → LoginPage (عام، يعيد توجيه إذا مسجل دخول)
/dashboard → DashboardPage (محمي، يتطلب تسجيل دخول)
```

### كيفية التنقل:

```javascript
import { useNavigate } from 'react-router-dom';

function MyComponent() {
  const navigate = useNavigate();
  
  const goToDashboard = () => {
    navigate('/dashboard');
  };
  
  return <button onClick={goToDashboard}>Dashboard</button>;
}
```

### كيفية الحصول على معلومات المسار:

```javascript
import { useLocation } from 'react-router-dom';

function MyComponent() {
  const location = useLocation();
  console.log('Current path:', location.pathname);
}
```

---

## متغيرات البيئة

### في التطوير:

```env
VITE_API_URL=http://localhost:5000
```

### في الإنتاج:

```env
VITE_API_URL=https://your-backend-api.onrender.com
```

### الوصول إلى المتغيرات في الكود:

```javascript
const apiUrl = import.meta.env.VITE_API_URL;
console.log('API URL:', apiUrl);
```

**ملاحظة**: في Vite، استخدم `import.meta.env` وليس `process.env`

---

## نصائح مهمة

### ✅ أفضل الممارسات:

1. **استخدم مكونات صغيرة** - كل مكون يقوم بمهمة واحدة
2. **احفظ Token بشكل آمن** - استخدم localStorage
3. **تعامل مع الأخطاء** - استخدم try/catch
4. **استخدم Loading States** - أظهر مؤشر تحميل
5. **تحقق من الاستجابات** - تأكد من نجاح الطلبات

### ⚠️ تجنب:

1. ❌ تخزين معلومات حساسة في الكود
2. ❌ عدم معالجة الأخطاء
3. ❌ نسيان حذف Token عند تسجيل الخروج
4. ❌ عدم استخدام Loading States

---

## الخطوات التالية

بعد إعداد البنية الأساسية:

- ✅ المهمة 6: إعداد Frontend - البنية الأساسية - **مكتملة**
- ⏭️ المهمة 7: بناء الصفحة الرئيسية (HomePage)
- ⏭️ المهمة 8: بناء صفحة تسجيل الدخول (LoginPage)
- ⏭️ المهمة 9: بناء مكون الحماية (ProtectedRoute) - **مكتمل**
- ⏭️ المهمة 10: بناء صفحة Dashboard

---

## الموارد

- [React Documentation](https://react.dev/)
- [Vite Documentation](https://vitejs.dev/)
- [React Router Documentation](https://reactrouter.com/)

---

**مبروك! البنية الأساسية للـ Frontend جاهزة! 🎉**
