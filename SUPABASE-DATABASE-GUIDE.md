# دليل نشر قاعدة البيانات على Supabase 🚀

هذا الدليل يشرح خطوة بخطوة كيفية إنشاء ونشر قاعدة بيانات PostgreSQL على Supabase.

---

## 📋 المحتويات

- [لماذا Supabase؟](#لماذا-supabase)
- [المتطلبات](#المتطلبات)
- [الخطوات](#الخطوات)
- [الاتصال بقاعدة البيانات](#الاتصال-بقاعدة-البيانات)
- [تشغيل init.sql](#تشغيل-initsql)
- [التحقق من النجاح](#التحقق-من-النجاح)
- [حل المشاكل](#حل-المشاكل)

---

## 🎯 لماذا Supabase؟

**Supabase** منصة Backend-as-a-Service قوية توفر:
- ✅ PostgreSQL مجاني (500 MB)
- ✅ إعداد سريع جداً (أقل من دقيقة)
- ✅ واجهة SQL Editor مدمجة
- ✅ نسخ احتياطية تلقائية
- ✅ SSL/TLS مدمج
- ✅ لوحة تحكم قوية
- ✅ API تلقائي لقاعدة البيانات
- ✅ Authentication مدمج (اختياري)

---

## 📦 المتطلبات

قبل البدء، تأكد من:
- ✅ حساب GitHub (للتسجيل في Supabase)
- ✅ ملف `backend/db/init.sql` جاهز

---

## 🚀 الخطوات

### الخطوة 1: إنشاء حساب على Supabase

1. اذهب إلى: **https://supabase.com**
2. اضغط **"Start your project"**
3. اختر **"Sign in with GitHub"**
4. وافق على الأذونات المطلوبة

---

### الخطوة 2: إنشاء Organization (اختياري)

1. بعد تسجيل الدخول، قد يُطلب منك إنشاء Organization
2. أدخل اسم Organization (مثل: `my-projects`)
3. اختر الخطة **"Free"**
4. اضغط **"Create organization"**

---

### الخطوة 3: إنشاء Project جديد

1. من Dashboard، اضغط **"New project"**
2. املأ المعلومات:

#### Project Name (اسم المشروع):
```
my-tiny-app
```

#### Database Password (كلمة مرور قاعدة البيانات):
```
أدخل كلمة مرور قوية (احفظها!)
مثال: MySecurePass123!@#
```
⚠️ **مهم جداً**: احفظ هذه الكلمة في مكان آمن!

#### Region (المنطقة):
اختر أقرب منطقة لك:
- **West EU (Ireland)** - أوروبا
- **East US (North Virginia)** - أمريكا الشرقية
- **West US (Oregon)** - أمريكا الغربية
- **Southeast Asia (Singapore)** - آسيا
- **Central EU (Frankfurt)** - أوروبا الوسطى

#### Pricing Plan:
اختر **"Free"**
- 500 MB Database
- 1 GB File Storage
- 2 GB Bandwidth
- 50,000 Monthly Active Users

3. اضغط **"Create new project"**
4. انتظر 1-2 دقيقة حتى يكتمل الإنشاء

---

## 🔗 الاتصال بقاعدة البيانات

بعد إنشاء المشروع، احصل على معلومات الاتصال:

### الخطوة 1: الذهاب إلى Database Settings

1. من القائمة الجانبية، اضغط **"Project Settings"** (أيقونة الترس ⚙️)
2. اختر **"Database"** من القائمة

---

### الخطوة 2: نسخ Connection String

ستجد عدة أنواع من Connection Strings:

#### Connection String (URI):
```
postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres
```

#### Connection Pooling (مُوصى به للإنتاج):
```
postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:6543/postgres?pgbouncer=true
```

**استخدم Connection String العادي** (المنفذ 5432) لمشروعنا.

---

### الخطوة 3: استبدال كلمة المرور

في Connection String، استبدل `[YOUR-PASSWORD]` بكلمة المرور التي أدخلتها عند الإنشاء:

```
postgresql://postgres:MySecurePass123!@#@db.xxxxx.supabase.co:5432/postgres
```

⚠️ **ملاحظة**: إذا كانت كلمة المرور تحتوي على رموز خاصة، قد تحتاج لترميزها (URL encoding).

---

## 📝 تشغيل init.sql

الآن نحتاج لتشغيل سكريبت `init.sql` لإنشاء الجداول والبيانات الأولية.

### الطريقة 1: باستخدام SQL Editor في Supabase (الأسهل!)

1. من Dashboard، اضغط **"SQL Editor"** في القائمة الجانبية
2. اضغط **"New query"**
3. افتح ملف `backend/db/init.sql` من جهازك
4. انسخ **كل** محتوى الملف
5. الصق المحتوى في SQL Editor
6. اضغط **"Run"** أو اضغط `Ctrl+Enter`
7. يجب أن ترى رسالة **"Success"**

---

### الطريقة 2: باستخدام psql (من جهازك)

#### Windows:

```bash
# افتح Command Prompt أو PowerShell
# استبدل CONNECTION_STRING بالرابط من Supabase
psql "CONNECTION_STRING" -f backend/db/init.sql
```

مثال:
```bash
psql "postgresql://postgres:MyPass123@db.xxxxx.supabase.co:5432/postgres" -f backend/db/init.sql
```

#### macOS/Linux:

```bash
psql "CONNECTION_STRING" -f backend/db/init.sql
```

---

### الطريقة 3: باستخدام pgAdmin

1. افتح **pgAdmin**
2. اضغط بزر الماوس الأيمن على **Servers** → **Register** → **Server**
3. في تبويب **General**:
   - Name: `Supabase - my-tiny-app`
4. في تبويب **Connection**:
   - Host: `db.xxxxx.supabase.co`
   - Port: `5432`
   - Database: `postgres`
   - Username: `postgres`
   - Password: كلمة المرور الخاصة بك
5. في تبويب **SSL**:
   - SSL Mode: `Require`
6. اضغط **Save**
7. افتح **Query Tool**
8. انسخ محتوى `backend/db/init.sql` والصقه
9. اضغط **Execute** (F5)

---

## ✅ التحقق من النجاح

بعد تشغيل `init.sql`، تحقق من نجاح العملية:

### 1. باستخدام Table Editor في Supabase:

1. من القائمة الجانبية، اضغط **"Table Editor"**
2. يجب أن ترى جدول **"users"**
3. اضغط على الجدول لعرض البيانات
4. يجب أن ترى المستخدم التجريبي **"demo"**

---

### 2. باستخدام SQL Editor:

```sql
-- عرض جميع الجداول
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public';

-- عرض المستخدمين
SELECT id, username, created_at FROM users;

-- عدد المستخدمين
SELECT COUNT(*) FROM users;
```

يجب أن ترى:
- جدول `users`
- مستخدم واحد: `demo`
- العدد: `1`

---

## 📋 حفظ معلومات الاتصال

**مهم جداً**: احفظ هذه المعلومات في مكان آمن:

### Connection String للاستخدام في Backend:

```
postgresql://postgres:YOUR_PASSWORD@db.xxxxx.supabase.co:5432/postgres
```

### معلومات منفصلة:

- **Host**: `db.xxxxx.supabase.co`
- **Port**: `5432`
- **Database**: `postgres`
- **Username**: `postgres`
- **Password**: كلمة المرور التي أدخلتها

⚠️ **لا تشارك هذه المعلومات أبداً!**

---

## 🔐 الأمان

### تفعيل Row Level Security (RLS) - اختياري

Supabase يوصي بتفعيل RLS للأمان:

1. اذهب إلى **"Authentication"** → **"Policies"**
2. اختر جدول `users`
3. اضغط **"Enable RLS"**

**ملاحظة**: لمشروعنا البسيط، RLS اختياري لأننا نستخدم JWT في Backend.

---

## 🐛 حل المشاكل

### المشكلة 1: "password authentication failed"

**السبب**: كلمة المرور خاطئة

**الحل**:
- تأكد من كلمة المرور الصحيحة
- إذا نسيتها، يمكنك إعادة تعيينها من:
  - Project Settings → Database → Reset Database Password

---

### المشكلة 2: "SSL connection required"

**السبب**: Supabase يتطلب SSL

**الحل**:
- أضف `?sslmode=require` في نهاية Connection String
- أو استخدم: `?ssl=true`

---

### المشكلة 3: "relation already exists"

**السبب**: الجداول موجودة بالفعل

**الحل**:
- هذا طبيعي إذا شغلت `init.sql` مرتين
- السكريبت يستخدم `IF NOT EXISTS` لتجنب الأخطاء
- يمكنك تجاهل هذا التحذير

---

### المشكلة 4: "too many connections"

**السبب**: وصلت للحد الأقصى من الاتصالات

**الحل**:
- استخدم Connection Pooling (المنفذ 6543)
- أو أغلق الاتصالات غير المستخدمة

---

## 📊 معلومات الخطة المجانية

### ما تحصل عليه:

- ✅ 500 MB Database Storage
- ✅ 1 GB File Storage
- ✅ 2 GB Bandwidth
- ✅ 50,000 Monthly Active Users
- ✅ SSL/TLS مدمج
- ✅ نسخ احتياطية يومية (7 أيام)
- ✅ Automatic backups
- ✅ SQL Editor
- ✅ Table Editor
- ✅ API تلقائي

### القيود:

- ⚠️ تتوقف بعد 7 أيام من عدم النشاط (يمكن إعادة تشغيلها)
- ⚠️ مساحة محدودة (500 MB)
- ⚠️ عدد اتصالات محدود

### للترقية:

إذا احتجت لمزيد من الموارد:
- **Pro**: $25/month - 8 GB Database, 100 GB Bandwidth
- **Team**: $599/month - مميزات متقدمة

---

## 🎯 الخطوة التالية

بعد إعداد قاعدة البيانات بنجاح:

✅ قاعدة البيانات جاهزة على Supabase
✅ الجداول مُنشأة
✅ المستخدم التجريبي موجود
✅ لديك Connection String

**التالي**: استخدام Connection String في Backend

---

## 💡 نصائح إضافية

### 1. استخدام Environment Variables:

في `backend/.env`:
```env
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.xxxxx.supabase.co:5432/postgres
```

### 2. مراقبة قاعدة البيانات:

- اذهب إلى **"Database"** → **"Logs"** لمشاهدة السجلات
- اذهب إلى **"Database"** → **"Reports"** للإحصائيات

### 3. النسخ الاحتياطية:

- Supabase يحفظ نسخ احتياطية تلقائياً
- يمكنك تحميل نسخة احتياطية من:
  - Project Settings → Database → Database Backups

### 4. API Keys:

- Supabase يوفر API تلقائي لقاعدة البيانات
- يمكنك استخدامه بدلاً من كتابة SQL
- اذهب إلى **"Project Settings"** → **"API"** للحصول على المفاتيح

---

## 🔗 روابط مفيدة

- [Supabase Documentation](https://supabase.com/docs)
- [Supabase SQL Editor](https://supabase.com/docs/guides/database/overview)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)
- [Connection Pooling](https://supabase.com/docs/guides/database/connecting-to-postgres#connection-pooler)

---

## 📝 ملاحظات مهمة

1. **احفظ كلمة المرور** - لا يمكن استرجاعها (فقط إعادة تعيين)
2. **استخدم SSL** - دائماً للأمان
3. **Connection String** - احفظه في `.env` فقط
4. **لا تشارك المعلومات** - معلومات حساسة جداً
5. **النسخ الاحتياطية** - Supabase يحفظها تلقائياً
6. **SQL Editor** - أسهل طريقة لتشغيل السكريبتات

---

**مبروك! قاعدة البيانات جاهزة على Supabase! 🎉**
