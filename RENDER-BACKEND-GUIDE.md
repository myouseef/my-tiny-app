# دليل نشر Backend على Render 🚀

## نظرة عامة

Render هي منصة استضافة حديثة توفر:
- ✅ نشر تلقائي من GitHub
- ✅ خطة مجانية (750 ساعة/شهر)
- ✅ HTTPS تلقائي
- ✅ سهلة الإعداد

---

## الخطوة 1: إنشاء حساب على Render

1. اذهب إلى: **https://render.com**

2. اضغط على **"Get Started"** أو **"Sign Up"**

3. اختر **"Sign up with GitHub"**

4. وافق على الصلاحيات المطلوبة

✅ **تم!** الآن لديك حساب على Render

---

## الخطوة 2: إنشاء Web Service

### 2.1 ابدأ إنشاء الخدمة

1. من لوحة التحكم، اضغط **"New +"**

2. اختر **"Web Service"**

3. اختر **"Build and deploy from a Git repository"**

4. اضغط **"Next"**

### 2.2 ربط مستودع GitHub

1. إذا لم يظهر مستودعك:
   - اضغط **"Configure account"**
   - اختر المستودعات التي تريد ربطها
   - أو اختر "All repositories"

2. ابحث عن مستودع `learning-deployment-project`

3. اضغط **"Connect"**

✅ **تم!** المستودع مربوط

---

## الخطوة 3: إعداد الخدمة

املأ المعلومات التالية:

### Name (الاسم)
```
my-tiny-app-backend
```
أو أي اسم تريده (سيكون جزء من الرابط)

### Region (المنطقة)
اختر الأقرب لك:
- `Frankfurt (EU Central)` - للشرق الأوسط وأوروبا
- `Singapore` - لآسيا
- `Oregon (US West)` - لأمريكا

### Branch (الفرع)
```
main
```
أو `master` حسب اسم الفرع الرئيسي لديك

### Root Directory (المجلد الرئيسي)
```
backend
```
⚠️ **مهم جداً!** هذا يخبر Render أن الكود في مجلد `backend`

### Runtime (بيئة التشغيل)
```
Node
```
سيتم اختياره تلقائياً

### Build Command (أمر البناء)
```
npm install
```

### Start Command (أمر التشغيل)
```
node server.js
```

### Instance Type (نوع الخادم)
```
Free
```
اختر الخطة المجانية

---

## الخطوة 4: إضافة Environment Variables

قبل النشر، نحتاج إضافة المتغيرات البيئية:

### 4.1 اضغط على "Advanced"

### 4.2 أضف المتغيرات التالية:

#### 1. DATABASE_URL
```
Key: DATABASE_URL
Value: postgresql://postgres.uprbqxwyrduvjflxkrkf:YOUR_PASSWORD@aws-0-REGION.pooler.supabase.com:6543/postgres
```
⚠️ **مهم جداً:** استخدم **Connection Pooling** (وليس Connection string العادي)

**كيفية الحصول عليه:**
1. Supabase Dashboard → Project Settings → Database
2. ابحث عن قسم **"Connection Pooling"** (وليس "Connection string")
3. اختر **"Session mode"**
4. انسخ Connection String

**يجب أن يحتوي على:**
- ✅ `pooler.supabase.com` في العنوان
- ✅ Port `6543` (وليس 5432)
- ✅ `postgres.PROJECT_REF` كاسم مستخدم

#### 2. JWT_SECRET
```
Key: JWT_SECRET
Value: your_super_secret_key_change_this_in_production_12345
```
⚠️ **استخدم كلمة سر قوية وفريدة!**

يمكنك توليد واحدة:
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

#### 3. NODE_ENV
```
Key: NODE_ENV
Value: production
```

#### 4. PORT (اختياري)
```
Key: PORT
Value: 5000
```
Render يوفر PORT تلقائياً، لكن يمكنك تحديده

---

## الخطوة 5: النشر

1. تأكد من جميع الإعدادات

2. اضغط **"Create Web Service"**

3. انتظر... سيبدأ Render في:
   - استنساخ المستودع
   - تثبيت المكتبات (`npm install`)
   - تشغيل الخادم (`node server.js`)

4. راقب Logs في الأسفل

5. عند النجاح، سترى:
   ```
   ✅ Database connected successfully
   🚀 Server running on http://0.0.0.0:10000
   Your service is live 🎉
   ```

✅ **مبروك!** Backend منشور على الإنترنت

---

## الخطوة 6: الحصول على رابط API

1. في أعلى الصفحة، ستجد رابط الخدمة:
   ```
   https://my-tiny-app-backend.onrender.com
   ```

2. انسخ هذا الرابط (ستحتاجه للـ Frontend)

---

## الخطوة 7: اختبار API

### 7.1 اختبار Health Endpoint

افتح المتصفح واذهب إلى:
```
https://your-app-name.onrender.com/api/health
```

يجب أن ترى:
```json
{
  "status": "ok",
  "database": "connected"
}
```

### 7.2 اختبار Login Endpoint

استخدم Thunder Client أو Postman:

**POST** `https://your-app-name.onrender.com/api/auth/login`

Headers:
```
Content-Type: application/json
```

Body:
```json
{
  "username": "demo",
  "password": "demo123"
}
```

يجب أن تحصل على:
```json
{
  "success": true,
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": 1,
    "username": "demo"
  }
}
```

✅ **ممتاز!** API يعمل بشكل صحيح

---

## الخطوة 8: تفعيل Auto-Deploy

Render يفعّل النشر التلقائي افتراضياً:

- عند كل `git push` إلى `main`
- سيعيد Render بناء ونشر التطبيق تلقائياً

للتحقق:
1. اذهب إلى **Settings**
2. ابحث عن **"Auto-Deploy"**
3. تأكد أنه **"Yes"**

---

## ملاحظات مهمة

### ⚠️ الخطة المجانية

- الخادم ينام بعد 15 دقيقة من عدم الاستخدام
- أول طلب بعد النوم قد يأخذ 30-60 ثانية
- 750 ساعة مجانية شهرياً (كافية لمشروع تعليمي)

### 🔒 الأمان

- لا تشارك JWT_SECRET مع أحد
- استخدم كلمات سر قوية
- راجع Logs بانتظام

### 📊 المراقبة

- **Logs**: لمراقبة الأخطاء والطلبات
- **Metrics**: لمراقبة الأداء والاستخدام
- **Events**: لمراقبة عمليات النشر

---

## استكشاف الأخطاء

### ❌ خطأ: "Build failed"

**الأسباب المحتملة:**
- Root Directory خاطئ (يجب أن يكون `backend`)
- package.json غير موجود
- خطأ في التبعيات

**الحل:**
- راجع Logs لمعرفة الخطأ بالضبط
- تأكد من Root Directory = `backend`
- تأكد من `package.json` موجود في `backend/`

### ❌ خطأ: "Database connection failed"

**الأسباب المحتملة:**
- DATABASE_URL خاطئ
- كلمة مرور Supabase خاطئة

**الحل:**
- راجع Environment Variables
- انسخ DATABASE_URL من Supabase مرة أخرى
- تأكد من استبدال `[YOUR-PASSWORD]`

### ❌ خطأ: "Port already in use"

**الحل:**
- احذف متغير PORT من Environment Variables
- Render يوفر PORT تلقائياً

### ❌ الخادم ينام بسرعة

**هذا طبيعي في الخطة المجانية!**
- الخادم ينام بعد 15 دقيقة من عدم الاستخدام
- أول طلب يوقظه (قد يأخذ 30-60 ثانية)

---

## الخطوات التالية

بعد نشر Backend بنجاح:

1. ✅ احفظ رابط API
2. ⏳ انتقل للمهمة 16: نشر Frontend على Vercel
3. ⏳ استخدم رابط Render في `VITE_API_URL`

---

## روابط مفيدة

- 📚 [Render Docs](https://render.com/docs)
- 🎥 [Render YouTube](https://www.youtube.com/c/Render)
- 💬 [Render Community](https://community.render.com/)

---

**مبروك! 🎉 Backend الخاص بك الآن على الإنترنت!**

**الخطوة التالية**: نشر Frontend على Vercel (المهمة 16)
